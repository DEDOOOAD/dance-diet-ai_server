# AI 서버 보고서 작성본

> 작성 범위: 본 문서는 메인 서버와 클라이언트를 제외하고, AI 서버 담당 부분만 보고서에 삽입할 수 있도록 정리한 내용이다. 코드 첨부를 최소화하기 위해 실제 구현 코드는 제외하고, 기능 목적, 처리 흐름, 사용 기술, 설계 의도를 중심으로 작성하였다.

## Ⅱ. 본론

### 1. 배경 지식

#### 가. FastAPI

FastAPI는 Python 기반의 웹 API 프레임워크로, 비동기 처리를 지원하며 데이터 검증과 API 문서화를 효율적으로 처리할 수 있다. 본 프로젝트의 AI 서버는 음식 이미지 분석 요청과 실시간 댄스 분석 요청을 처리해야 하므로, 요청 처리 속도와 명확한 데이터 구조가 중요하다. FastAPI는 Pydantic 모델을 기반으로 요청과 응답 데이터를 검증할 수 있고, HTTP API뿐만 아니라 WebSocket 통신도 함께 지원하기 때문에 AI 서버 구현에 적합하다.

본 프로젝트에서는 음식 이미지 분석 기능은 HTTP POST 방식으로 제공하고, 댄스 분석 기능은 WebSocket 방식으로 제공한다. 음식 분석은 하나의 이미지를 전달받아 분석 결과를 반환하는 구조이므로 일반적인 REST API 방식이 적절하다. 반면 댄스 분석은 여러 프레임을 연속적으로 전달받고 분석 결과를 실시간으로 다시 보내야 하므로, 연결을 유지한 상태에서 데이터를 주고받을 수 있는 WebSocket 방식이 적절하다.

#### 나. WebSocket

WebSocket은 클라이언트와 서버가 한 번 연결을 맺은 뒤, 연결을 유지하면서 양방향으로 데이터를 주고받을 수 있는 통신 방식이다. 일반적인 HTTP 요청은 요청과 응답이 한 번씩 오간 뒤 연결이 종료되는 구조이지만, WebSocket은 연결이 유지되기 때문에 실시간성이 필요한 기능에 적합하다.

본 프로젝트의 댄스 분석 기능에서는 메인 서버가 사용자의 춤 영상 프레임을 AI 서버로 전달하고, AI 서버는 각 프레임을 분석하여 칼로리 소모량과 움직임 점수를 다시 전송한다. 이 과정에서 매 프레임마다 HTTP 요청을 새로 생성하면 통신 비용이 커지고 지연 시간이 증가할 수 있다. 따라서 WebSocket을 사용하여 하나의 세션 안에서 프레임 수신, 분석, 결과 송신을 반복하도록 설계하였다.

#### 다. 컴퓨터 비전과 OpenCV

컴퓨터 비전은 이미지나 영상 데이터를 컴퓨터가 이해하고 분석할 수 있도록 처리하는 기술이다. AI 서버에서는 사용자의 음식 사진과 춤 영상 프레임을 모두 이미지 데이터로 다루기 때문에 컴퓨터 비전 처리가 핵심적인 역할을 한다.

OpenCV는 이미지 디코딩, 색상 변환, 프레임 처리 등 컴퓨터 비전 작업에 널리 사용되는 라이브러리이다. 본 프로젝트에서는 클라이언트 또는 메인 서버로부터 Base64 형태로 전달된 이미지 데이터를 바이트 배열로 변환한 뒤, OpenCV를 이용해 실제 이미지 배열로 디코딩한다. 음식 분석에서는 디코딩된 이미지를 음식 분류 모델에 입력하고, 댄스 분석에서는 디코딩된 프레임을 포즈 분석 모델에 입력한다.

#### 라. MediaPipe Pose Landmarker

MediaPipe Pose Landmarker는 이미지나 영상에서 사람의 신체 주요 관절 위치를 추정하는 기술이다. 사람의 어깨, 팔꿈치, 손목, 허리, 무릎, 발목 등 여러 신체 부위의 좌표를 추출할 수 있으며, 이를 통해 사용자의 자세와 움직임을 정량적으로 분석할 수 있다.

본 프로젝트에서는 댄스 영상 프레임에서 포즈 랜드마크를 추출하고, 현재 프레임의 관절 위치와 이전 프레임의 관절 위치 차이를 계산하여 움직임 정도를 산출한다. 단순히 사람이 화면에 있는지만 확인하는 것이 아니라, 프레임 간 신체 좌표 변화량을 기반으로 움직임 점수를 계산하기 때문에 춤 동작의 활동량을 수치로 표현할 수 있다.

#### 마. YOLO 기반 음식 분류

YOLO는 이미지를 한 번에 분석하여 객체의 종류와 위치를 예측하는 컴퓨터 비전 모델 계열이다. 본 프로젝트의 음식 분석 기능에서는 Ultralytics YOLO 모델을 사용하여 입력 이미지에서 음식 후보를 분류한다. 모델은 음식 이미지에 대해 여러 개의 후보 라벨과 신뢰도를 반환하며, AI 서버는 그중 설정된 기준 이상의 후보를 분석 결과로 사용한다.

음식 사진은 촬영 각도, 조명, 음식의 배치, 반찬 구성 등에 따라 예측 난이도가 달라진다. 따라서 AI 서버는 단일 결과만 반환하기보다 상위 후보를 함께 고려할 수 있도록 구성되어 있다. 이를 통해 음식 인식 결과의 유연성을 높이고, 분류 모델이 확신하기 어려운 상황에서도 보조 분석 로직을 적용할 수 있다.

#### 바. 멀티모달 비전 모델

멀티모달 비전 모델은 텍스트와 이미지를 함께 이해할 수 있는 AI 모델이다. 일반적인 이미지 분류 모델은 학습된 라벨 범위 안에서 결과를 예측하는 데 강점이 있지만, 복합적인 음식 구성이나 한국 음식처럼 형태가 다양한 대상에서는 한계가 발생할 수 있다.

본 프로젝트에서는 음식 분류 모델의 결과를 보완하기 위해 Gemini 또는 OpenAI 기반의 비전 분석을 fallback 구조로 사용할 수 있도록 설계하였다. 비전 모델은 이미지 속 음식명을 한국어로 반환하고, 보이는 1인분 기준의 예상 칼로리와 신뢰도를 함께 제공한다. 이 방식은 기존 분류 모델이 알 수 없는 음식이나 복합 음식 이미지를 보완하는 역할을 한다.

#### 사. MET 기반 칼로리 계산

MET는 Metabolic Equivalent of Task의 약자로, 신체 활동의 에너지 소비 강도를 나타내는 단위이다. 휴식 상태를 기준으로 활동 강도가 높을수록 MET 값이 증가한다. 운동이나 춤과 같은 활동의 칼로리 소모량은 사용자의 체중, 활동 시간, 활동 강도에 따라 달라진다.

본 프로젝트의 댄스 분석 기능에서는 Ainsworth et al.의 2011 Compendium of Physical Activities와 2024 Adult Compendium of Physical Activities의 Dancing 항목을 참고하였다. 해당 자료는 신체 활동별 MET 값을 제시하며, 댄스 활동의 경우 느린 볼룸댄스 3.0 MET, 일반 컨템포러리 댄스 3.8 MET, 일반 발레·모던·재즈 수업 5.0 MET, 비디오 기반 살사 6.3 MET, 강도 높은 발레·모던·재즈 6.8 MET, 플라멩코 8.5 MET, 격렬한 나이트클럽·포크댄스 9.8 MET 등으로 구분된다.

본 프로젝트는 특정 댄스 장르를 사용자가 매번 직접 선택하는 구조가 아니므로, 위 MET 값을 고정값으로 하나만 적용하지 않았다. 대신 MediaPipe로 추출한 프레임 간 포즈 랜드마크 변화량을 움직임 점수로 계산하고, 움직임 점수에 따라 Compendium의 댄스 MET 단계 중 하나를 선택하도록 설계하였다. 칼로리 계산식은 `kcal/min = MET × 3.5 × 체중(kg) / 200`을 사용하며, 실제 프레임 간 경과 시간에 맞추어 초 단위 소모량으로 변환한다. 또한 휴식 상태에서 발생하는 1 MET를 제외한 active MET를 별도로 계산하여, 사용자가 춤 동작으로 추가 소모한 칼로리에 가깝게 추정하도록 하였다.

### 2. 시스템 구축 내용

#### 가. AI 서버 전체 구성

AI 서버는 Python 기반 FastAPI 애플리케이션으로 구성되어 있으며, 메인 서버 또는 클라이언트로부터 전달되는 이미지 및 영상 프레임 데이터를 분석하는 역할을 담당한다. 전체 구조는 요청을 받는 Controller 계층, 실제 기능을 수행하는 Service 계층, AI 모델을 실행하는 AI 모듈 계층, 칼로리 계산 및 데이터 조회를 담당하는 Calorie 계층으로 구분된다.

| 구분 | 주요 역할 |
| --- | --- |
| Controller | HTTP 및 WebSocket 요청 수신, 요청 데이터 검증, 오류 응답 처리 |
| Service | 음식 분석 및 댄스 분석의 전체 처리 흐름 관리 |
| Food AI | 음식 이미지 분류, 비전 fallback 분석, 이미지 전처리 |
| Dance AI | 포즈 랜드마크 추출, 움직임 점수 계산, 칼로리 계산 |
| Calorie | 음식 칼로리 데이터 로딩 및 음식명 기반 칼로리 조회 |
| Config | 모델 경로, 서버 포트, 외부 AI 모델명, 환경 변수 관리 |
| Schema | 요청 및 응답 데이터 구조 정의 |

사진 삽입 위치: AI 서버 전체 구조도

AI 서버는 기능별 책임을 분리하여 유지보수성을 높였다. 예를 들어 음식 분석 요청을 받는 부분은 `food_controller`가 담당하고, 실제 음식 분석 흐름은 `food_service`가 담당한다. 음식 분류 모델 실행은 `food_classifier`가 담당하며, 칼로리 조회는 `food_calorie`가 담당한다. 댄스 분석도 마찬가지로 WebSocket 요청 수신, 프레임 정렬, 포즈 분석, 칼로리 계산의 책임이 분리되어 있다.

#### 나. 애플리케이션 초기화

AI 서버는 실행 시점에 필요한 리소스를 미리 초기화한다. 서버가 시작되면 음식 분류 모델을 로딩하고, 음식 칼로리 데이터베이스를 메모리에 적재한다. 또한 댄스 분석을 위한 포즈 탐지기 세션을 관리할 수 있도록 세션 저장 공간을 준비한다.

이러한 초기화 방식은 요청이 들어올 때마다 무거운 AI 모델이나 데이터 파일을 반복해서 로딩하지 않기 위한 설계이다. AI 모델은 로딩 비용이 크기 때문에 매 요청마다 불러오면 응답 속도가 크게 저하될 수 있다. 따라서 서버 시작 시 한 번 로딩한 뒤 애플리케이션 상태에 보관하고, 각 요청에서는 이미 로딩된 객체를 의존성 주입 방식으로 받아 사용한다.

초기화 과정에서 모델 파일이나 음식 데이터 파일이 존재하지 않으면 예외를 발생시켜 서버 실행 단계에서 문제를 빠르게 확인할 수 있도록 하였다. 이는 실제 서비스 중간에 분석 요청이 실패하는 것을 줄이고, 배포 환경에서 필요한 리소스 누락을 조기에 발견하는 데 도움이 된다.

#### 다. 음식 이미지 분석 기능

음식 이미지 분석 기능은 사용자가 촬영한 음식 사진을 바탕으로 음식 종류와 예상 칼로리를 반환하는 기능이다. 메인 서버는 음식 이미지를 Base64 문자열로 변환하여 AI 서버에 전달하고, AI 서버는 이를 분석한 뒤 음식명, 칼로리, 신뢰도, 총 칼로리를 응답한다.

1) 요청 데이터 수신 및 검증

음식 분석 API는 `POST /api/food/analyze` 엔드포인트를 통해 제공된다. 요청 데이터에는 사용자 또는 분석 대상을 식별하기 위한 UUID, Base64로 인코딩된 이미지, 선택적으로 이미지 파일명이 포함된다.

AI 서버는 먼저 Base64 문자열이 올바른 형식인지 검증한다. 잘못된 Base64 데이터가 전달되면 이미지 디코딩이 불가능하므로 400 오류를 반환한다. 또한 디코딩 결과가 비어 있는 경우에도 정상적인 이미지가 아니라고 판단하여 요청을 거부한다. 이 검증 단계는 AI 모델에 잘못된 입력이 전달되는 것을 막고, 오류 원인을 명확하게 구분하기 위한 과정이다.

2) 이미지 디코딩

Base64 검증이 끝나면 AI 서버는 이미지 문자열을 바이트 데이터로 변환한다. 이후 OpenCV를 이용해 바이트 데이터를 이미지 배열로 디코딩한다. 디코딩된 이미지는 음식 분류 모델이 처리할 수 있는 입력 형태가 된다.

이미지 데이터는 네트워크를 통해 전달되는 과정에서 손상될 수 있고, 사용자가 이미지가 아닌 데이터를 전달할 수도 있다. 따라서 디코딩 결과가 비어 있거나 이미지 배열이 생성되지 않는 경우에는 분석을 중단하고 알 수 없는 결과로 처리한다.

3) YOLO 모델 기반 음식 후보 분류

디코딩된 이미지는 Ultralytics YOLO 기반 음식 분류 모델에 전달된다. 모델은 이미지에서 음식 후보를 예측하고, 각 후보에 대해 음식 라벨과 신뢰도를 반환한다. AI 서버는 설정된 상위 개수와 최소 신뢰도 기준을 바탕으로 후보 목록을 구성한다.

모델이 확률 정보를 제공하는 경우에는 상위 후보를 사용하고, 탐지 박스 정보를 제공하는 경우에는 감지된 객체의 신뢰도를 기준으로 후보를 정렬한다. 이처럼 분류 결과와 탐지 결과를 모두 처리할 수 있도록 구성하여 모델 출력 형태가 달라져도 분석 흐름이 유지되도록 하였다.

4) 비전 fallback 분석

음식 이미지는 한국 음식의 특성상 한 이미지 안에 여러 반찬이 함께 있거나, 음식의 모양이 일정하지 않은 경우가 많다. 이 경우 일반 분류 모델만으로는 정확한 음식명을 반환하기 어려울 수 있다. 이를 보완하기 위해 AI 서버는 Gemini 또는 OpenAI 기반 비전 분석을 fallback 방식으로 사용할 수 있도록 설계하였다.

비전 fallback은 이미지 속에서 보이는 음식 항목을 한국어 음식명으로 반환하고, 각 음식의 예상 칼로리와 신뢰도를 함께 제공한다. fallback 결과가 존재하는 경우 AI 서버는 해당 결과를 우선 사용한다. fallback 결과가 없거나 외부 API 키가 설정되어 있지 않은 경우에는 YOLO 분류 모델의 결과를 사용한다.

이 구조는 외부 AI 서비스가 항상 필수로 동작하지 않아도 서버가 기본 분석 기능을 수행할 수 있게 하며, 외부 비전 모델이 사용 가능한 환경에서는 음식 인식 정확도를 보완할 수 있게 한다.

5) 음식 칼로리 매핑

음식명이 결정되면 AI 서버는 음식 칼로리 데이터베이스에서 해당 음식명의 칼로리를 조회한다. 음식 칼로리 데이터는 CSV 또는 Excel 파일에서 로딩되며, 서버 시작 시 메모리에 사전 형태로 저장된다. 음식명은 공백을 제거하고 소문자 기준으로 정규화하여 조회한다.

fallback 분석에서 이미 칼로리 값이 제공된 경우에는 해당 값을 우선 사용한다. fallback 칼로리가 없거나 0인 경우에는 서버의 음식 칼로리 데이터베이스를 조회한다. 데이터베이스에도 음식명이 존재하지 않으면 0으로 처리하고 로그를 남긴다. 이를 통해 분석 결과는 반환하되, 칼로리 데이터 누락 여부를 개발자가 확인할 수 있도록 하였다.

6) 응답 데이터 생성

분석이 완료되면 AI 서버는 음식 항목 리스트와 총 칼로리를 계산하여 응답한다. 각 음식 항목에는 음식명, 칼로리, 신뢰도가 포함된다. 전체 응답에는 UUID, 음식 목록, 총 칼로리, 이미지 파일명, 분석 출처, 분석 시간이 포함된다.

이 응답 구조는 메인 서버가 결과를 저장하거나 클라이언트 화면에 표시하기에 적합하도록 구성되어 있다. 특히 총 칼로리는 서버에서 한 번 계산하여 반환하므로, 클라이언트 또는 메인 서버가 동일한 계산을 반복하지 않아도 된다.

사진 삽입 위치: 음식 이미지 분석 처리 흐름도

#### 라. 댄스 실시간 분석 기능

댄스 실시간 분석 기능은 사용자의 춤 영상 프레임을 분석하여 움직임 점수와 칼로리 소모량을 계산하는 기능이다. 사용자의 영상 전체를 한 번에 업로드하는 방식이 아니라, 프레임을 순차적으로 전달받아 분석하므로 실시간 피드백이 가능하다.

1) WebSocket 연결 및 세션 생성

댄스 분석은 `WS /ws/dance/analyze/{session_id}` 엔드포인트를 통해 제공된다. 메인 서버가 특정 세션 ID로 WebSocket 연결을 요청하면, AI 서버는 연결을 수락한 뒤 해당 세션을 위한 포즈 탐지기를 생성한다. 연결이 완료되면 AI 서버는 `ai_ready` 메시지를 전송하여 분석 준비가 끝났음을 알린다.

세션 ID는 여러 사용자의 분석 흐름을 구분하기 위한 값이다. 같은 서버에서 여러 분석 세션이 동시에 진행될 수 있으므로, AI 서버는 세션 ID를 기준으로 포즈 탐지기와 이전 프레임 상태를 분리하여 관리한다.

2) 프레임 메시지 수신 및 검증

WebSocket 연결 이후 메인 서버는 `frame_base64` 타입의 메시지를 AI 서버로 전송한다. 메시지에는 세션 ID, 프레임 인덱스, 전체 프레임 수, Base64 이미지, 사용자 체중, 사용자 키가 포함될 수 있다.

AI 서버는 먼저 메시지 타입이 올바른지 확인한다. `ping` 메시지가 들어오면 서버 준비 상태를 다시 응답하고, 잘못된 타입이 들어오면 오류 메시지를 반환한다. 이후 Pydantic 모델을 이용해 필수 필드와 값의 범위를 검증한다. 예를 들어 프레임 인덱스는 0 이상이어야 하며, 사용자 체중과 키가 전달되는 경우 양수여야 한다.

또한 WebSocket 경로에 포함된 세션 ID와 메시지 내부의 세션 ID가 일치하는지도 확인한다. 이 검증은 다른 세션의 프레임이 잘못 섞이는 것을 방지하기 위한 장치이다.

3) 프레임 순서 보정

실시간 프레임 전송 과정에서는 네트워크 지연이나 처리 속도 차이로 인해 프레임이 순서대로 도착하지 않을 수 있다. AI 서버는 프레임 인덱스를 기준으로 다음에 처리해야 할 프레임 번호를 관리한다. 아직 처리할 순서가 아닌 프레임은 임시 저장소에 보관하고, 다음 순서의 프레임이 도착하면 연속된 프레임을 순서대로 처리한다.

이미 처리된 프레임보다 오래된 프레임이 뒤늦게 도착하면 해당 프레임은 stale frame으로 판단하여 건너뛴다. 이 방식은 포즈 변화량 계산에서 프레임 순서가 뒤바뀌는 문제를 줄여 준다. 움직임 점수는 이전 프레임과 현재 프레임의 차이를 기반으로 계산되기 때문에, 프레임 순서를 유지하는 것은 분석 정확도에 중요한 요소이다.

4) 포즈 랜드마크 추출

정상적으로 디코딩된 프레임은 MediaPipe Pose Landmarker에 입력된다. OpenCV에서 디코딩한 이미지는 BGR 색상 형식이므로, MediaPipe에 전달하기 전에 RGB 형식으로 변환한다. 이후 Pose Landmarker는 프레임에서 사람의 신체 랜드마크를 추출한다.

포즈가 감지되지 않는 경우에는 움직임 점수와 칼로리 소모량을 0으로 반환한다. 포즈가 감지되는 경우에는 현재 프레임의 랜드마크 좌표를 배열 형태로 변환하고, 이전 프레임의 랜드마크 좌표와 비교한다.

5) 움직임 점수 계산

움직임 점수는 현재 프레임과 이전 프레임의 포즈 랜드마크 좌표 차이를 평균한 값이다. 각 관절 좌표의 변화량을 계산한 뒤 전체 랜드마크의 평균 변화량을 사용한다. 이 값이 클수록 사용자의 신체 움직임이 크다고 판단할 수 있다.

첫 번째 프레임은 비교할 이전 프레임이 없으므로 움직임 점수를 0으로 처리한다. 이후 프레임부터는 이전 랜드마크 정보가 세션 상태에 저장되어 있으므로, 프레임 간 변화량을 계산할 수 있다.

사용자의 키 정보가 전달된 경우에는 기준 키에 대한 비율을 적용하여 움직임 점수를 보정한다. 같은 동작이라도 사용자의 신체 크기에 따라 좌표 변화량이 다르게 나타날 수 있으므로, 키 정보를 반영하여 활동량 계산의 일관성을 높이고자 하였다.

6) MET 및 칼로리 계산

AI 서버는 움직임 점수를 바탕으로 현재 활동 강도를 추정한다. 움직임 점수가 매우 낮은 경우에는 휴식 상태에 가까운 1.0 MET로 처리하고, 일정 기준 이상 움직임이 감지되면 2024 Adult Compendium의 Dancing 항목에 제시된 실제 MET 값 중 가장 적절한 강도 단계를 선택한다. 적용한 기준값은 3.0, 3.8, 4.5, 5.0, 6.0, 6.3, 6.8, 7.3, 8.5, 9.8 MET이다.

칼로리 계산에는 선택된 MET 값, 사용자 체중, 프레임 간 경과 시간이 사용된다. 표준 MET 칼로리 계산식은 `MET × 3.5 × 체중(kg) / 200`이며, 이는 1분 기준 에너지 소비량을 의미한다. AI 서버는 프레임 간 경과 시간을 초 단위로 계산하므로, 해당 값을 분 단위로 변환하여 프레임별 칼로리 소모량을 산출한다. 사용자 체중이 전달되지 않으면 기본 체중값을 사용하며, 체중이 g 단위로 전달된 것으로 보이는 경우 kg 단위로 변환한다. 비정상적으로 작거나 큰 체중 값은 기본값으로 보정한다.

프레임 간 경과 시간은 서버의 타임스탬프를 기준으로 계산하되, 비정상적으로 긴 지연 시간이 칼로리 계산에 과도하게 반영되지 않도록 최대 경과 시간을 제한한다. 이를 통해 네트워크 지연이나 일시적인 프레임 누락이 칼로리 계산 결과를 크게 왜곡하지 않도록 하였다.

7) 분석 결과 전송

분석 결과는 `ai_analysis` 타입의 메시지로 전송된다. 응답에는 세션 ID, 처리 시간, 해당 프레임에서 계산된 칼로리 소모량, 움직임 점수가 포함된다. 메인 서버는 이 결과를 받아 사용자에게 실시간 피드백으로 제공하거나, 누적 기록을 계산하는 데 사용할 수 있다.

WebSocket 연결이 종료되면 AI 서버는 해당 세션의 포즈 탐지기를 닫고, 세션 상태를 삭제한다. 이 과정은 서버 메모리 누수를 방지하고, 다음 분석 세션이 이전 사용자의 상태에 영향을 받지 않도록 하기 위한 처리이다.

사진 삽입 위치: 댄스 실시간 분석 처리 흐름도

#### 마. 데이터 모델 구조

AI 서버는 Pydantic 모델을 사용하여 요청 및 응답 데이터 구조를 명확하게 정의하였다. 데이터 모델을 별도로 정의하면 Controller에서 들어오는 데이터의 형식을 자동으로 검증할 수 있고, Service 계층에서는 이미 검증된 값을 기준으로 비즈니스 로직을 처리할 수 있다.

| 모델 | 역할 |
| --- | --- |
| LiveFrameMessage | 댄스 분석용 실시간 프레임 메시지 구조 정의 |
| AiLiveAnalysisMessage | 댄스 분석 결과 메시지 구조 정의 |
| FoodAnalysisRequest | 음식 분석 요청 데이터 구조 정의 |
| FoodItem | 음식 분석 결과의 개별 음식 항목 구조 정의 |
| FoodAnalysisResponse | 음식 분석 전체 응답 구조 정의 |

이러한 데이터 모델은 메인 서버와 AI 서버 사이의 계약 역할을 한다. 즉, 어떤 필드가 필요하고 어떤 형식으로 응답이 반환되는지 명확하게 정해 두기 때문에, 서버 간 연동 과정에서 발생할 수 있는 데이터 형식 오류를 줄일 수 있다.

#### 바. 예외 처리 및 로그 관리

AI 서버는 분석 과정에서 발생할 수 있는 오류를 단계별로 구분하여 처리한다. 음식 분석에서는 Base64 형식 오류, 빈 이미지 데이터, 이미지 디코딩 실패, 모델 분석 실패 등을 구분한다. 댄스 분석에서는 메시지 타입 오류, 세션 ID 불일치, 프레임 검증 실패, 이미지 디코딩 실패, WebSocket 연결 종료 등을 처리한다.

특히 분석 요청과 결과 전송 과정에는 로그를 남기도록 구성하였다. 음식 분석에서는 이미지 바이트 크기, 후보 음식명, 신뢰도, 총 칼로리 등을 기록한다. 댄스 분석에서는 세션 연결, 프레임 수신, 프레임 디코딩, 분석 완료, 결과 전송, 연결 종료를 기록한다. 이러한 로그는 오류가 발생했을 때 원인을 추적하거나, 실제 운영 중 분석 흐름이 정상적으로 진행되는지 확인하는 데 활용된다.

#### 사. AI 서버 설계의 주안점

1) 기능별 책임 분리

AI 서버는 Controller, Service, AI 모듈, Calorie 모듈을 분리하여 구성하였다. Controller는 요청과 응답을 담당하고, Service는 기능 흐름을 조율하며, AI 모듈은 모델 실행과 이미지 분석을 담당한다. 칼로리 데이터 조회는 별도 모듈로 분리하여 음식 분석 로직과 데이터 조회 로직이 섞이지 않도록 하였다.

이 구조는 SOLID 원칙 중 단일 책임 원칙과 관련이 있다. 하나의 파일이나 함수가 모든 역할을 담당하지 않고, 변경 이유가 서로 다른 기능을 분리함으로써 유지보수성을 높였다. 예를 들어 음식 칼로리 데이터 형식이 바뀌더라도 음식 이미지 분류 모델 자체를 수정할 필요가 없고, WebSocket 메시지 검증 방식이 바뀌더라도 포즈 분석 알고리즘 전체를 수정하지 않아도 된다.

2) 확장 가능한 AI 모델 구조

음식 분석 기능은 기본적으로 YOLO 분류 모델을 사용하지만, 필요할 경우 외부 비전 모델을 fallback으로 사용할 수 있다. 또한 fallback 제공자를 설정값으로 분리하여 Gemini와 OpenAI 중 선택할 수 있도록 구성하였다. 이는 향후 모델 성능, 비용, 응답 속도에 따라 다른 비전 모델을 적용할 수 있는 확장성을 제공한다.

댄스 분석 기능도 포즈 탐지기를 세션별로 생성하고 관리하는 구조이므로, 추후 더 정교한 자세 평가 모델이나 동작 분류 모델을 추가할 수 있다. 현재는 포즈 랜드마크 변화량을 기반으로 움직임 점수와 칼로리를 계산하지만, 향후에는 특정 춤 동작의 정확도, 박자 일치율, 자세 안정성 같은 지표로 확장할 수 있다.

3) 실시간성과 안정성 고려

댄스 분석은 실시간 프레임 처리가 핵심이므로 WebSocket을 사용하였다. 또한 프레임 인덱스를 기준으로 순서를 보정하고, 오래된 프레임은 건너뛰도록 하여 분석 흐름이 뒤섞이지 않도록 설계하였다. 포즈 탐지기와 세션 상태는 연결 종료 시 정리하여 서버 자원이 불필요하게 유지되지 않도록 하였다.

음식 분석은 요청 단위로 독립적인 결과를 반환하므로 HTTP API로 구성하였다. 이미지 입력 오류와 모델 처리 오류를 구분하여 응답하고, 서버 로그를 통해 문제 상황을 확인할 수 있도록 하였다.

4) 메인 서버와의 연동성

AI 서버는 메인 서버와 독립적으로 실행되며, 메인 서버는 필요한 시점에 AI 서버 API를 호출하거나 WebSocket으로 연결한다. 이 방식은 AI 모델의 실행 환경과 메인 서버의 사용자 관리, 데이터 저장 로직을 분리할 수 있다는 장점이 있다.

AI 서버는 분석에 집중하고, 메인 서버는 사용자 인증, 기록 저장, 클라이언트 응답 조립을 담당할 수 있다. 역할이 분리되어 있기 때문에 AI 서버의 모델을 교체하거나 성능 개선을 진행하더라도 메인 서버 전체 구조를 크게 변경하지 않아도 된다.

### 3. 테스트 및 검증

AI 서버는 이미지와 실시간 프레임을 다루기 때문에 정상 입력뿐만 아니라 비정상 입력 상황도 고려해야 한다. 음식 분석 기능에서는 올바른 Base64 이미지가 전달되는 경우 음식 목록과 총 칼로리가 반환되는지 확인한다. 잘못된 Base64 문자열, 빈 이미지, 디코딩할 수 없는 이미지가 전달되는 경우 적절한 오류가 반환되는지도 확인한다.

댄스 분석 기능에서는 WebSocket 연결이 정상적으로 수립되고, 연결 직후 `ai_ready` 메시지가 반환되는지 확인한다. 이후 프레임 메시지를 순서대로 전송했을 때 `ai_analysis` 메시지가 반환되는지 확인한다. 또한 프레임 인덱스가 뒤섞여 도착하는 상황, 세션 ID가 불일치하는 상황, 메시지 타입이 잘못된 상황에서도 서버가 연결을 유지하며 오류 메시지를 반환하는지 검증한다.

성능 측면에서는 AI 모델 로딩 시간이 요청 처리 시간에 반복적으로 포함되지 않는지 확인해야 한다. 서버 시작 시 모델과 칼로리 데이터가 한 번만 로딩되고, 이후 요청에서는 기존 리소스를 재사용하는 구조이므로 반복 요청 시 응답 시간이 안정적으로 유지되는지 확인할 수 있다.

### 4. 결론

#### 가. AI 서버 개발 결과

본 프로젝트의 AI 서버는 음식 이미지 분석과 댄스 실시간 분석이라는 두 가지 핵심 기능을 제공한다. 음식 분석 기능은 사용자가 촬영한 음식 이미지를 기반으로 음식명, 예상 칼로리, 신뢰도, 총 칼로리를 반환한다. 댄스 분석 기능은 사용자의 춤 영상 프레임을 실시간으로 분석하여 움직임 점수와 칼로리 소모량을 계산한다.

AI 서버는 FastAPI를 기반으로 HTTP API와 WebSocket API를 함께 제공하며, 각 기능에 적합한 통신 방식을 선택하였다. 음식 분석은 요청과 응답이 명확한 REST API로 구성하고, 댄스 분석은 실시간 프레임 처리가 필요한 특성을 고려하여 WebSocket으로 구성하였다.

또한 AI 모델 실행, 데이터 검증, 칼로리 계산, 세션 관리 기능을 분리하여 유지보수성과 확장성을 확보하였다. 이 구조는 향후 음식 분류 모델 개선, 외부 비전 모델 교체, 댄스 자세 평가 기능 추가와 같은 확장 작업을 비교적 안정적으로 수행할 수 있게 한다.

#### 나. 기대 효과

첫째, 사용자는 음식 사진만으로 식단의 예상 칼로리를 확인할 수 있다. 이는 사용자가 직접 음식명을 입력하거나 칼로리를 검색해야 하는 번거로움을 줄여 주며, 식단 관리 접근성을 높인다.

둘째, 댄스 활동 중 발생하는 움직임을 기반으로 칼로리 소모량을 실시간으로 확인할 수 있다. 단순한 운동 시간 기록이 아니라 실제 프레임 간 신체 움직임을 반영하므로, 사용자의 활동 정도를 더 직관적으로 파악할 수 있다.

셋째, AI 서버와 메인 서버가 분리되어 있기 때문에 서비스 구조가 명확하다. AI 서버는 모델 실행과 분석에 집중하고, 메인 서버는 사용자 정보와 서비스 데이터를 관리할 수 있다. 이 구조는 팀 단위 개발에서 역할 분담을 쉽게 하고, 기능별 유지보수를 용이하게 한다.

#### 다. 개선 방안

첫째, 음식 분석 정확도 향상을 위해 음식 데이터셋을 지속적으로 보강할 필요가 있다. 한국 음식은 조리 방식과 반찬 구성이 다양하기 때문에, 실제 사용자 이미지 데이터를 기반으로 모델을 재학습하거나 라벨 체계를 세분화하면 정확도를 높일 수 있다.

둘째, 음식 칼로리 데이터베이스의 음식명 정규화가 필요하다. 같은 음식이라도 표기 방식이 다를 수 있으므로 동의어, 띄어쓰기, 한글 표기 차이를 처리하는 매핑 테이블을 추가하면 칼로리 조회 실패를 줄일 수 있다.

셋째, 댄스 분석 기능에서는 움직임 점수뿐만 아니라 동작 정확도나 박자 일치율을 추가 지표로 제공할 수 있다. 현재는 프레임 간 랜드마크 변화량을 기반으로 활동량을 계산하지만, 향후 기준 안무 데이터와 사용자의 포즈를 비교하면 자세 교정 기능으로 확장할 수 있다.

넷째, 실시간 분석 성능 향상을 위해 프레임 샘플링 전략을 개선할 수 있다. 모든 프레임을 분석하는 방식은 정확도는 높지만 서버 부하가 증가할 수 있다. 일정 FPS 기준으로 분석 프레임을 선택하거나, 움직임이 큰 구간을 우선 분석하는 방식을 적용하면 효율을 높일 수 있다.

다섯째, 서버 운영 안정성을 위해 테스트 코드와 모니터링 체계를 강화할 필요가 있다. 이미지 디코딩 실패, 외부 비전 API 실패, 모델 파일 누락, WebSocket 연결 종료 등 주요 예외 상황에 대한 자동화 테스트를 추가하면 배포 안정성을 높일 수 있다.

## Ⅳ. 참고문헌

1. FastAPI 공식 문서, https://fastapi.tiangolo.com/
2. FastAPI WebSocket 공식 문서, https://fastapi.tiangolo.com/advanced/websockets/
3. Pydantic 공식 문서, https://docs.pydantic.dev/latest/
4. Google AI Edge, MediaPipe Pose Landmarker Python Guide, https://ai.google.dev/edge/mediapipe/solutions/vision/pose_landmarker/python
5. OpenCV 공식 문서, Image file reading and writing, https://docs.opencv.org/4.x/d4/da8/group__imgcodecs.html
6. Ultralytics YOLO Python Usage 공식 문서, https://docs.ultralytics.com/usage/python/
7. NumPy 공식 문서, https://numpy.org/doc/stable/
8. pandas 공식 문서, https://pandas.pydata.org/docs/
9. Ainsworth, B. E., Haskell, W. L., Herrmann, S. D., Meckes, N., Bassett, D. R., Tudor-Locke, C., Greer, J. L., Vezina, J., Whitt-Glover, M. C., & Leon, A. S. (2011). 2011 Compendium of Physical Activities: A Second Update of Codes and MET Values. Medicine & Science in Sports & Exercise, 43(8), 1575-1581. DOI: 10.1249/MSS.0b013e31821ece12.
10. 2024 Adult Compendium of Physical Activities, Dancing MET table, https://pacompendium.com/dancing/

## 부록: 보고서 삽입 시 활용할 수 있는 요약 문장

AI 서버는 사용자의 음식 이미지와 댄스 영상 프레임을 분석하여 식단 및 운동 정보를 수치화하는 역할을 수행한다. 음식 분석 기능은 Base64 이미지 데이터를 입력받아 음식명을 예측하고, 음식 칼로리 데이터베이스 또는 비전 fallback 결과를 활용하여 총 칼로리를 계산한다. 댄스 분석 기능은 WebSocket을 통해 프레임을 실시간으로 수신하고, MediaPipe Pose Landmarker로 추출한 신체 랜드마크의 변화량을 기반으로 움직임 점수와 칼로리 소모량을 계산한다. 이러한 구조를 통해 AI 서버는 메인 서버와 독립적으로 동작하면서도, 사용자에게 식단 관리와 운동 분석에 필요한 핵심 AI 분석 결과를 제공한다.
